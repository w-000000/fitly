spring:
  datasource:
    url: jdbc:h2:mem:minip;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  h2:
    console:
      enabled: true
server:
  error:
    include-message: always
springdoc:
  swagger-ui:
    path: /swagger-ui.html

